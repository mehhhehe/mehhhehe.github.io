# Mehreen Mansoor

Personal portfolio site for GitHub Pages.

This is a small static site, consisting of HTML, CSS, and vanilla JavaScript. The main profile content lives in `assets/data.js`, while paper notes live in `assets/papers.js` and `posts/`.

<!-- ## Local Preview

Run a local server from this folder:

```bash
python3 -m http.server 8010
```

Then open:

```text
http://localhost:8010
```

## GitHub Pages

This repo is intended to be published as:

```text
mehreen_m.github.io
```

After pushing to GitHub, enable Pages from the repository settings:

```text
Settings -> Pages -> Deploy from a branch -> main -> /root
```

The site entry point is `index.html`.

## Updating Content

Edit:

- `assets/data.js` for profile, experience, projects, education, and links.
- `assets/papers.js` for the notes index.
- `posts/*.html` for full note pages.

Private projects can stay listed without exposing their repositories by leaving `link: ""` and using a `status`, for example:

```js
{
  name: "Private Project",
  stack: "Python · Flask",
  desc: "Short public-safe description.",
  link: "",
  status: "private repo"
}
``` -->
